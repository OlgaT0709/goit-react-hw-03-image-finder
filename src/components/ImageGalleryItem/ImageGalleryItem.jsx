import PropTypes from 'prop-types';
import { } from './ImageCalleryItem.styled';



export const ImageCalleryItem = ({}) => {
    return (
        <>
            
        </>
    );
}

ImageCalleryItem.propTypes = {
        
};